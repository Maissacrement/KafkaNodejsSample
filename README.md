# Template de base Kafka Nodejs

```bash
  run : `docker compose up --build`
  rm : `docker-compose stop && docker-compose rm -f`
```

```bash
Techno :
  - `Kafka`
  - `Docker`
  - `Nodejs`
  - `Typescript`
  - `mocha`
```
```bash
Requirement:
- Docker
- Nodejs 10
```
